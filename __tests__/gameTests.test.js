


describe('stateGuesser', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('stateScramble', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('wordProblem', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('hangman', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('radar', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('rockPaperScissors', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('ticTacToe', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('maze', () => {
  it('correct answer equals the expected, and on fail loops gamestate', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});

describe('chatroom', () => {
  it('loops chat room, displays messages dynamically, and pulls from queue and server properly', async () => {
    const response = 1;

    expect(response).toBeTruthy();
  });
});